{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Guardar" class="btn btn-primary"/>
        <a href="{{ path('menu_index') }}" class="btn btn-default" >Cancelar</a>
    {{ form_end(edit_form) }}
{% endblock %}
